name: Ci

on:
  pull_request:




jobs:

  list_runners:
    runs-on: ubuntu-latest
    steps:
      - name: List runners
        run: |
          echo "Available runners:"
          gh api repos/ezratameno/lets_go/actions/runners 
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 


  setup:
    runs-on: pldm
    strategy:
      matrix:
        dummy: [dummy1, dummy2]
    outputs:
      runner_name: ${{ runner.name }}
    steps:
      - name: Print runner name
        run: echo Set up runner ${{ runner.name }}, with dummy ${{ matrix.dummy }}


  test:
    runs-on: ${{ needs.setup.outputs.runner_name }}
    needs: setup
    strategy:
      matrix:
        dummy: [dummy1, dummy2]
    steps:
      - name: Print runner name
        run: echo "Running on ${{ runner.name }}, with dummy ${{ matrix.dummy }}"
